#include<iostream>
#include<vector>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
	cin.tie(0);

    int n = 0,
        k = 0;
    
    cin >> n;
    cin >> k;

    if(k % 2 != 0) {
        cout << ;
    } else {

    }
    return 0;
}